# 平台无关特性树与节点差异确认方案

版本：v2。本文替代原“叶子等于一篇差异知识、目录全挂载即完成”的方案。

## 1. 最终目标

构建一棵以平台无关语义定义的特性树，以每个节点为比较单元，持续补充并确认 Android、iOS、HarmonyOS 的支持情况与行为差异。

树回答“比较什么”；节点平台记录回答“能否完成、在什么条件下完成”；差异记录回答“指定维度是否相同、不同，哪些判断已经核实”。最终浏览入口合并这三部分。

- 节点定义和父子划分独立于某个平台的 API 目录。
- 每个节点都允许平台比较，包括 L1/L2 父节点。
- 支持、差异、证据核实进度和文章编辑状态独立表达。
- 缺乏证据时保留 unknown；没有记录不表示没有差异。
- 只有一个平台提供的能力同样进入公共树。

## 2. 比较节点与粒度

每个节点有稳定 ID、中英文名称、平台无关定义、包含与排除范围、比较维度和一个父节点。父子关系表示能力分类或细分；同组兄弟节点采用一致的切分轴。横切关系用 related_features 或正文引用表达。

拆分的依据是独立语义与可独立比较的任务/子能力。平台差异可以暴露值得拆出的子能力，但权限名称、类名、参数顺序不同本身不足以要求拆分。文章篇幅不再决定节点能否存在。

例：BLE 扫描可分为扫描过滤、后台扫描、批量结果交付。过滤条件与批量交付不能互作缺失子能力。过滤叶子应明确是在比较系统过滤、应用层过滤、过滤条件组合还是交付结果，避免默认以某个平台的 API 全集定义 full。

父节点只在其自身范围内比较整体能力、架构或共同约束，链接子节点的细项；父节点结论不能自动继承到子节点，也不能用“一项子能力存在”推出整域支持。

L1/L2 等只表示深度，不预设终止层数。新增平台发现了新的独立能力时可扩展树；现有节点无需因平台模型名称改变而重建。

## 3. 数据与权威来源

| 数据 | 权威来源 | 说明 |
| --- | --- | --- |
| 节点语义、范围、父子关系 | taxonomy YAML | 保持一棵主树，脚本不再内置第二份 FEATURES |
| 每个平台的支持和确认状态 | 对应 knowledge.presence | 通过 feature_id 附着到节点，不在 taxonomy 重复存储断言 |
| 每维度、每平台对的差异 | knowledge.comparisons | 一条记录对应一个维度和一个无序平台对 |
| 原始目录及多对多关联 | inventory catalogs | 候选、确认、目录归类、能力关联分别标记 |
| 精选 API 绑定 | taxonomy.bindings | 保留编辑者挑选的入口，绑定本身不证明支持程度 |
| 页面/矩阵/反向索引/进度 | bindings 与 reports | 可重建，合并上述信息 |

初期注册三个平台；平台和维度放在 config/comparison.yaml，新增平台不需修改核心 Schema。当前比较采用一个明确的版本基线/运行上下文；历史版本差异保留正文与证据，切换基线后相关确认必须重新核实。

## 4. 三种状态分别维护

平台支持：unknown / supported / conditional / unsupported。supported 表示在声明范围内满足任务；conditional 必须描述条件。unsupported 必须有明确证据，不能根据“没有找到 API”推断。

差异结果：unknown / same / different / not_applicable。按支持范围、编程模型、生命周期与后台、权限与隐私、限制与精度、设备形态、API 形态逐项记录。

确认进度：unreviewed / in_review / confirmed。它与结果独立，核实中的 different 仍是假设。confirmed 必须有范围、平台上下文、证据、确认者与日期。

文章仍保留 stub / draft / reviewed，表示编辑状态。文章 reviewed 不代表所有平台、所有维度都已确认。旧 full/partial/analog/none/unique 原样迁入 legacy_presence，正文保留待复核。

analog 属于编程模型比较；独占性应由所选平台范围内已经确认的支持情况推导，不能混入单个平台的支持枚举。

## 5. 证据与版本

确认平台支持时填写 context：发行版、系统版本、SDK 版本、设备形态及必要条件。确认一个平台对的差异时，两边都须有可追溯证据，且证据 applies_to 与对应 context 一致。

证据记录官方 URL、具体章节/符号 locator、固定 revision。引用本地文档时保存 local_path 与 SHA-256，并在审计时检查快照变化。快照哈希只能证明使用了哪份文件，不能自行证明它属于指定 SDK。

OpenHarmony 的 master 文档不能直接证明 HarmonyOS 某个版本的支持。Apple 全站目录不能直接视为 iOS SDK。基线尚未固定的知识可以处于核实中，不能确认。

自动审计检验契约、引用与可追溯性；语义结论仍需要根据证据实质审核。

## 6. 目录映射与覆盖率

采集单元保留平台原名、文档来源及哈希。scope 分 included / excluded / pending；visibility 独立表示 public / system 等访问属性。范围排除不得伪装成 duplicate。

自动规则仅生成 candidate，不产生 confirmed。关系分为 catalog（目录归类）和 capability（能力关联）。允许同一 API 关联多个节点，也允许关联父节点的整体能力；但必须明确关联语义。

删除全局 runtime/app 兜底；未知目录进入待核实清单。规则目标不存在时直接报错，禁止退回祖先冒充成功。人工映射标记 method: manual，后续规则刷新保留。

覆盖率分别统计：范围待定、候选目录归类、候选能力关联、已确认能力关联、未映射。目录映射率不代表树完整或差异确认完成。系统、测试和本期排除的语言基础库不计入能力确认范围。

## 7. 构建和编辑流程

1. 定义节点任务、边界与本层比较维度；确定一个父节点。
2. 关联原始文档和候选 API，保留待确认范围。
3. 为每个平台核实支持条件和明确上下文，记录证据。
4. 按维度比较每个平台对；确认相同也需要证据。
5. 把可独立比较的子能力拆出，将具体知识放到对应节点，父节点引用。
6. 运行 refresh 和审计，查看剩余确认项，按域持续补充。

自动生成器只创建缺失知识。已存在的正文、draft、reviewed、注释和格式均不覆盖。节点定义修订时同步知识记录，审计检查漂移，避免两套定义并存。

## 8. 验收

结构验收：Schema、重复 ID、父子关系、环、深度、角色、知识归属、比较定义、平台注册、证据引用、快照一致性、目录范围及完整反向索引全部有效。

研究进度：按节点统计已确认平台支持数量、已确认维度×平台对数量。任意维度发现已确认差异可标记 has_confirmed_differences，但仍显示未完成进度；只有全部相关项与支持状态完成后才能标记 confirmed_same。

scripts/audit_coverage.py 默认验收结构，成功不宣称研究完成。增加 --require-complete 后，所选节点仍有待确认项返回 2；结构错误返回 1。--domain 限定研究验收子树，结构检查仍覆盖整个仓库。

## 9. 本次落地与后续工作

已落地：v2 Schema、全节点比较、历史数据迁移、安全生成器、候选多对多映射、证据与结构审计、完整索引、全节点矩阵和回归测试。

现有 212 个节点均已迁移，原知识保留。蓝牙、跨应用协作、UI 已提供核实中的比较样例。历史草稿并未据此完成事实确认；全量版本核实、树语义补全和各节点的证据审核仍按域推进。最新数量以 output/reports/progress.md 为准。
