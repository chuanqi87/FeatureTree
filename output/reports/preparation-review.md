# 全量分析前准备审查与修复

审查日期：2026-09-05。范围采用用户选择：启动日最新正式版、手机优先，其他形态单独注明。

当前研究输入可用，但尚未达到全量分析放行条件。实际检查和逐节点任务见 [启动报告](readiness.md) 与 [机器可读清单](readiness.json)。

## 已修复的问题

| 问题 | 修改结果 |
| --- | --- |
| 官网 AI 生成的 Page Summary 混入正文 | 提取器排除明确的 AI 摘要面板；从原始响应重新处理 1,305 份缓存，保留真实 API/指南正文和原始快照 |
| Google/Firebase 文档按厂商全部归为 Android | 修正 660 个路径明确属于 iOS 的来源；候选筛选另排除明确不属于目标平台的资料，空白适用性仍保留未知 |
| 已有绑定没有官方 URL，部分 API 正文未下载 | 阅读对应官方正文后补齐 35 条导航绑定；新增下载 6 份 API 正文；绑定缺口由 36 条降到 1 条 |
| 下载器忽略 macOS 系统代理 | 正文和目录下载统一使用已有代理配置；修复 NO_PROXY 单独存在时屏蔽系统代理的问题，已用真实补抓验证 |
| 确认记录可只填 URL 和版本字符串 | 加入官方来源校验、正文摘录匹配、获取时间、文件哈希、版本/设备适用性说明；事实可逐条引用证据 |
| 差异、独有或相同的断言缺少调查边界 | confirmed 记录区分官方明示与证据分析；unsupported、same、not_applicable 需要 coverage_note |
| 浏览器与部分导出跳过证据校验 | 浏览器、矩阵导出、树导出使用统一确认校验；失效确认不能继续展示或导出为已确认；完整性失败时研究完成状态为 false |
| 上下文和复核结果可能过期 | 候选包绑定节点、配置与来源指纹；检查原始响应、正文、摘录行号；运行清单记录知识、版本基线和来源快照指纹 |
| 并发输出与延迟失败可能损坏状态 | 文件输出采用独立临时文件再原子替换；迟到的失败响应不会覆盖另一请求已经保存的可读正文 |
| 全量洞察可能重复统计父子节点 | 明确将 106 个叶子与 106 个父级汇总分开计数；保留 unknown 分母，禁止继承父子结论 |

## 仍需在研究启动时处理

1. 用启动日官方发布资料锁定三个平台的正式系统与 SDK，保存 `output/research/baseline.json`。已登记官方检索入口；当前没有把检索结果、历史 baseline 或本机工具版本伪装成完成的版本核验。
2. 完成 BLE 过滤、后台任务、运行时权限、声明式 UI、分布式通信五个试点并记录复核。允许有理由和具体 missing_requirements 的 unknown，避免为了通过门槛制造确定答案。
3. 核实 `distributed.softbus.fabric` 的示意绑定 `SystemCapability.DistributedHardware.DistributedHardware`；它尚无核实过的官方对应 URL，不能作为已确认公开 API 使用。
4. 在逐节点研究中复核 201 个尚未填写 excludes 的节点边界，以及所有节点的比较维度。排除项不能机械填充，父级也不能复制子节点结论。
5. 继续处理目录的 392 个已纳入但未映射项（Android 112、HarmonyOS 280）及 iOS 154 个范围待定项。现有 212 节点的全量分析不等于三个 SDK 的完整能力普查。

已保存响应 36,386 份、目录快照 26 份通过原文完整性校验；仍有待下载或失败 URL，数量见 [资料验收](official-documents/progress.md)。这是资料状态，不是语义覆盖或版本确认。

验证通过：73 项 Python 回归测试、10 项前端测试，以及 212 节点的 848 个详情面板渲染。启动命令的严格模式预期返回 2，原因是版本基线和试点尚待完成。

后续执行方式见 [全量研究启动契约](../../docs/research-runbook.md)。
