# FeatureTree v2

最终交付是每个叶子的 Android、iOS、HarmonyOS 能力对照知识，含差异、条件、实现依据、逐结论可信度和人工审核状态。树定义知识范围，API 与官方原文提供证据；具名 Agent 分阶段分析，确定性代码管理校验、计数、状态、发布。

## 目标与边界

- 首版仅公开系统 SDK（含 Native）应用开发接口；生态 SDK 后续扩展。公开性、发行版、设备和来源体系分别核实。
- 允许重设计 L1；两千叶子仅规模参考。粒度按分领域校准的核心 API 符号族、实现路线和独立比较价值判断，不设 40/50 硬阈值。
- 公共骨架、代表叶子、知识样稿与评级标尺须用户确认后批量扩展；按领域冻结，冻结后生产正式知识。校准样稿可使用固定候选快照，必须标为样稿。
- 整体可信度为全部必答结论最低档。未知为低可信；已知错误、虚构证据不能靠低评级放行。低可信可见、可检索并进入人工队列。
- 未找到不等于不支持；相同、不支持、独有、不适用需检查范围和替代路径。能力结果和接口形态分别比较。
- 置信度、执行成功、人工裁决和真机验证是不同维度。真机结果须真实材料。

## 唯一真相与旧版

正式读取固定 data/CURRENT.json 指向的不可变发布。来源快照、业务对象、发布清单封存后不修改；审核事件追加记录。索引和导出可重建，不是事实写入入口。
新运行仅用 .workflow/v2/runs/。旧源码在 codex/archive-v1-20260910-091230；旧数据在 archive/v2-cutover-20260910-091230/。旧树、运行、冻结、基线和评级仅作历史参考，不迁移为成功状态，不重启旧执行链。
原文库 docs-raw/official/ 保留。备份必须覆盖 Git 忽略的原文、data、运行和归档。

## 模块与接口

依赖方向：corpus → core；taxonomy → core/corpus；knowledge → core/corpus/taxonomy；workflow 和 reporting → 上述四者；console → workflow/reporting 及下层只读接口；cli 负责装配。
core 不含业务语义；corpus 不建公共树；taxonomy 不调用模型；knowledge 不调度；reporting 不改事实；console 不绕过用例写数据。
业务输入显式传入接口和对象，不使用隐式全局 Repository。17 个 Agent 的权限、输入输出和返工归属固定于注册表，独立审查只消费已提交产物。

## 工程约定

遵循单一职责、高内聚低耦合；副作用与纯计算分离；关键规则可测试。重大架构变更先给方案，本次方案已获批准。保留用户修改，不留占位 TODO 或失效兼容写入链。脚本为薄入口；共享测试数据在 tests/fixtures/，测试不跨套件导入。后端、前端、架构和端到端验收共同约束切换。
