# 一日首次粗分析：启动记录

用户授权调整：提高 OpenCode 并发，第一轮先要全量初步结果，不要求逐项精审。目标截止北京时间 **2026-09-07 11:26**，对应 2026-09-06 约 11:26 开始准备的一天窗口。

- 范围：现有 212 节点，106 叶子和 106 父级。一次节点调查，不是 5,088 个知识项的完整精审。
- 模型：`volcengine/glm-5.3`，研究任务由 Agent Bridge 派发。
- 并发：4 路冒烟（后台任务、BLE过滤、运行时权限、声明式UI）；至少3份机器交付合格后升8路。连续失败先退避/降并发，八次连续失败停止新增派发。
- 时间控制：每节点约6分钟软预算、约15分钟取消阈值，整个批次受上述截止限制；允许轮询延迟，不是计费硬上限。
- 次数控制：每节点一次调用；不中途换模型或自动重跑。发起回执不确定则停下来核对已有job。
- 交付：三平台初判、来源、版本与范围缺口、最多三条差异假设、文档证据强弱、真机需求分类和精研优先级。无需详细真机计划。
- 诚实边界：置信度low，文档信号不是最新正式版支持确认；机器通过不是独立内容通过。首轮产物不写入正式knowledge，不改变生产启动门槛。

批次：`output/research/first-pass/18fd5314e6f04b10a56a34a98dc68cdaf7fd155c5f737208b7292f08b5bab411/`。

实时查看其 `report.md`、`index.json` 和 `state.json`。叶子尝试全部结束后开始父级；父级仅研究自身范围，不自动继承全部子节点判断。失败和未运行保留在全量分母中。

## 启动时验证

159 项 Python 回归测试通过，其中本轮19项覆盖引文/评级、输入失效、4→8并发、单实例锁、断点续接、禁止重复派发、超时取消与父级顺序。研究输入就绪，正式bulk门槛仍为false，符合隔离首轮契约。

真实4路均已获得job_id，并已分别生成各自目录的context，没有争写全局候选汇总。运行器PID在 `runner-process.json`，不能仅凭PID文件认定进程仍活着。运行期间通过单独的caffeinate进程临时阻止空闲休眠；合盖、断网、服务端限流或额度仍可能影响目标。

已配置本任务的20分钟跟进：只在完成、失败、阻塞或需要用户选择时通知；完成后核对实际结果并作跨领域内容抽查。跟进不自动升级confirmed、不自动追加模型重试，结束后暂停。

代码检查点：`149e6f0`，未推送。运行期间冻结代码、研究规则与正式知识，产物在隔离批次中继续生成。

## 单独记录的一次 CLI 探测误调用

能力检查时 `task --help` 被 Agent Bridge 解析为只读模型任务，job=`task-mtp935xs-imif16`，session=`ses_f8b3ebf4dffehmxHDHdUTLWtft`，现已完成，只返回帮助提示，没有生成研究内容。该探测未显式指定模型，实际默认模型/账单在此未核实，不能算作GLM5.3研究样本或免费调用。它与上述212节点研究调用单独计数；后续不再以task子命令探测帮助。
