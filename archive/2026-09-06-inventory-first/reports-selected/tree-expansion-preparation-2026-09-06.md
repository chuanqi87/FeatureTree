# 特性树优先：任务分解能力调整验收

日期：2026-09-06。此次只修改执行能力并做本地验证，没有调用 OpenCode 或启动新的分析。

## 目标与防偏机制

核心交付是几千个细粒度节点的三端能力树，每节点附实现粗知识、来源、置信度与真机需求。保留 25 个 L1 种子；历史 212 个节点不作为范围上限或完成度分母。

新的阶段为：官方目录盘点 → 独立树审查 → L2/L3领域骨架 → 已审分支递归细分 → 跨域合并 → 全树质量验收。旧节点补知识入口必须显式选择 --existing-nodes-only，用户停止的旧批次是终态。

作者一包可以提交多个新节点，而不是只回答旧节点。包内最多60节点为上下文限制；未完成能力必须保留待细分分支，不能截断后声称完成。独立原子能力叶子默认以2000作为起始规模门槛，数量不替代覆盖和语义质量，不按API方法/参数凑数。

验收拦截：漏目录组、无证据排除、旧L2+无去向、环/孤儿、重复ID/语义、同级划分冲突、原子节点仍有孩子、缺三端实现记录、伪实测、越级置信度、来源哈希/行号错误。全树验收另拦截未审任务、未细分分支、缺节点知识、已知范围缺口和低于约定规模。机器检查不能证明语义完整；必须有绑定精确产物的独立审核。

## 真实目录零模型准备

最新计划：[manifest.json](/Users/legend/Desktop/FeatureTree/output/research/tree-expansion/1a784a67b682055f05753d9479631b9924276a482c549ec21e2cc69dbff77cfb/manifest.json)。

- 模型配置：volcengine/glm-5.3；实际提交：0；在途：0；状态：prepared。
- 本地已发现官方 URL：184383。包含未下载、失败、范围待定等，不能视为已完成正文或全部能力。
- 导航分片：7051，只用于调度，不是特性节点。
- 首阶段目录盘点包：178，Android 22、iOS 15、HarmonyOS 141。
- 当前只生成 inventory 包。未形成新的真实特性节点，没有把目录数量转成特性完成数。
- 后续领域/递归包按独立审查结果动态生成，不固定为178个或212个任务。

通过 prepare/status/catalog 的真实数据检查；未审核时执行 advance 返回预期错误“Catalog routing needs independent review before domain outlines”，没有绕过关卡派生领域包。

## 本地回归

189项 Python 回归测试通过，其中29项为新树流程测试，另增加1项旧批次停止终态测试。合成夹具验证递归到L7、每节点三端知识、跨域去重、父提案变化使后代失效、精确引用、审核绑定、全树独立验收、8路并发上限、累计调用预算、丢失收据不重复派发、停止竞争条件与停止不重启。合成夹具不是实际平台研究，也不是GLM吞吐实测。

git diff --check 通过。正式 taxonomy/、knowledge/ 和 config/research.yaml 未改。候选运行数据独立保存并排除出默认Git跟踪；代码和协议可正常审查。本轮未自动提交Git。

## 当前执行状态与下一步

旧批次保持 stopped_by_user，自动跟进保持 PAUSED。新批次没有执行记录，也没有恢复模型调用。

新流程首次实跑应先执行跨三平台的4包目录样板，独立验收后扩大盘点；领域骨架与递归细分也需先完成一个有原子叶子的样板。当前最新正式版/SDK基线仍未核实，知识只能low/unassessed，不可冒充高可信；这不允许降低树结构质量。新的实际调用预算、截止时间和吞吐需要在真实样板后确认，不延用旧212任务的2–4小时估计。

操作与审核协议：[tree-expansion.md](/Users/legend/Desktop/FeatureTree/docs/tree-expansion.md)。

