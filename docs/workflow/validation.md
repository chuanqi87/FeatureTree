# 工作流 v1 验证记录（2026-09-09）

本记录验证执行框架，不评定真实能力树的研究质量。

| 验证 | 结果 |
| --- | --- |
| Python 完整回归 | 92 个测试通过，其中新工作流测试 19 个 |
| 前端回归 | 15 个测试通过 |
| 页面渲染检查 | 29 个节点的 145 个详情面板通过，来源文本转义通过 |
| 当前树 lint | 0 error、0 warning |
| refresh 完整性检查 | 通过；新增知识空壳 0；研究未完成 |
| OpenCode 加载 | 本机 1.18.29 正确加载全部 7 个 ft-* agent；编辑、shell、嵌套派单、交互提问均被禁用 |
| 真实模型协议 smoke | ft-scope 返回合法 JSON 信封，通过 task_id/input_hash/stage 与 Schema 检查；用时 45.296 秒 |

真实 smoke 成功产物保存在 `.workflow/smoke/20260909-174647-677237/`。先前一次调用在 120 秒预算处超时；将烟测预算设为 300 秒后再次执行成功。记录的是实际观测，不推断供应商超时原因或保证后续延迟。生产 task 默认超时 600 秒，可配置。

新增测试覆盖：多个节点和三端共享并发上限、瞬时失败与预算耗尽、成功阶段复用、局部与整批审查退回、定向返工、中断状态恢复、旧进程身份核对与清理、篡改产物、失效规则、过期树、无锚点和错误结构、候选未处置、越界路径、锚点正文哈希/符号边界、幂等发布、部分发布后的恢复，以及外部编辑阻止恢复。

发布测试使用临时目录中的合成候选，不包含真实平台支持断言。真实 smoke 只测试协议。当前正式数据仍为 29 个 L1、0 个 atomic 叶子，未启动节点批量研究、未冻结树。下一项研究质量验收应是小批量真实节点试点，按流程规范检查候选覆盖、边界、粒度与来源适用性。

运行器当前面向本机 macOS/Linux（依赖文件锁、进程组与 ps）；不声称支持 Windows 或跨机器分布式调度。
