# v2 工程检查点

2026-09-10。本记录说明已经执行的工作，不代表正式架构切换或知识生产完成。

- M0：9d3e35a 保存切换清单；旧代码归档分支和忽略数据已逐项验证。
- 建立 core 的内容哈希、不可变对象、持久原子 IO、显式 Schema 注册、恢复型单指针发布底座。
- 建立 36 份对象和阶段 Schema、17 个 Agent、两个具名流水线和两个代码阶段。
- 历史来源导入快照：s_d1790f818b1179557425e05156694ed2cc47e75a1d6f1867ba50fb9703a5045f。531,112 条规范声明、531,112 个保守单声明符号族、11,069 主题、184,383 文档目录项。4,999 条规范身份碰撞保留在 provenance.identity_collisions；原始输入字节独立保存于 data/imports，不依赖参考项目运行。
- 来源查询使用可重建 SQLite 索引；分页固定快照和选择条件，正文检查哈希；原文保持原位。
- 新运行器有显式 DAG、具名 Agent 固定配置、独立进程工作区、尝试和事件、限额、定向失效、审查和评级处理器。
- 模型替身跑通 17 个 Agent，得到 9 个必答结论的低可信校准测试文章。测试材料仅存在临时目录，不产生正式发布。

验证：`.venv/bin/python -m unittest tests.backend.test_v2_storage tests.backend.test_v2_pipeline -q`，11 项通过。

尚待完成：完整 SDK 适配器与稳定版基线；真实协议/三个范围校准；正式冻结和发布门的全套负例；运行器并发、格式修复、资料补充与证据依赖强化；人工修订与发布装配；新版 CLI/HTTP/前端；删除旧执行链；其余测试迁移、完整端到端、文档和用户校准确认。不能据此宣布 M1—M8 全部完成。
